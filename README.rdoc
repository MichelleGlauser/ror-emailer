This Ruby on Rails emailer allows a page to take a message and send it to the provided email address.

The only thing that needs to be adjusted on an individual basis at this point is the config/smtp_setup.rb file, where you'll need to enter your own domain, user_name, and password.